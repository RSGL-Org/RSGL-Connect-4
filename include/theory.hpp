#define fmax 3 //how many moves should it look ahead?

int aiChoice(std::vector<RSGL::color>){
    for (int y=0; y < fmax; y++){
        for (int x=0; x < collums; x++){
            
        }
    }
    return 0;
}